<% content_header do -%>
  <%= h(truncate(Project.current.name)) %> / 
  <%= link_to _('Milestone') + ' ' + h(truncate(@milestone.name)), goals_path %> 
  <% unless @sprint.new_record? -%>
     / <%= link_to h(truncate(@sprint.title)), sprint_location %>
  <% end -%>
<% end -%>
