<%=_ "Dear user!" %>

<%=_ "You are receiving this message because you have applied for an user account at %{site}.", :site => @site_name %>

<%=_ "Your registration details" %>:

   <%= (_("Username") + ':').ljust(20) %><%=h @user.username %>
   <%= (_("Full name") + ':').ljust(20) %><%=h @user.name %>
   <%= (_("Email") + ':').ljust(20) %><%=h @user.email %>

<%=_ "To complete the registration process you need to verify your email address. To make up this process you have two options" %>:

<%=_ "1. Click this link" %>:
   <%= account_activate_url(:username => @user.username, :code => @user.activation_code) %>
   <%=_ "Alternatively, copy and paste this link to your browser address bar." %>

<%=_ "2. Surf to %{url} and enter the following activation code", :url => account_activate_url %>: <%= @user.activation_code %>
