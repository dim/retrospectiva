<tfoot>
  <tr>
    <th colspan="<%= colspan.first || 1 %>"><%= will_paginate(collection, :inner_window => 3) %></th>
    <th colspan="<%= colspan.last  || 1 %>" class="righted"><%= will_paginate_per_page(collection) %></th>
  </tr>
</tfoot>